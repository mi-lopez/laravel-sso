# Copy to .env (gitignored) and adjust values for your local environment.
# These are read by phpunit.xml when running tests locally.

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=laravel_sso_test
DB_USERNAME=postgres
DB_PASSWORD=
